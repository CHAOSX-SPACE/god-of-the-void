## What this forges

<!-- One or two lines. What changes, and why it was not right before. -->

## Measured, not assumed

```
bash run-tests.sh
```

- [ ] Both editions carry it (ES **and** EN)
- [ ] `run-tests.sh` is green, and I pasted the total it summed
- [ ] Every number I wrote came from a command, not from memory
- [ ] New commands are in the `ES_CMDS` / `EN_CMDS` lists
- [ ] At least one test **sabotages** the new behaviour and demands the red

## If this cures a fault

Cite the file and the exact string in the cure, in backticks, so
`chaos faults --probe` can close it on evidence rather than on faith.
